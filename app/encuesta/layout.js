/** Layout dedicado: viewport para teclado móvil (Chrome/Android) sin tocar el resto del sitio. */
export const viewport = {
  interactiveWidget: "resizes-content",
};

export default function EncuestaLayout({ children }) {
  return children;
}
